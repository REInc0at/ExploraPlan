<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.ExploraPlan" parent="android:Theme.Material.Light.NoActionBar" />
    <!-- Base application theme. -->
    <style name="Base.Theme.ExploraPlan" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ExploraPlan.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.ExploraPlan.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.ExploraPlan.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    
    <style name="Them.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/background_color</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/exploraplan_round</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.ExploraPlan</item>

    </style>
</resources>